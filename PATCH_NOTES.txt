ORGANON FORCE MOBILE VIEW PATCH

Files changed:
- index.html

Files included unchanged for convenience:
- tools.json

Surgical changes made:
- Added a Force Mobile View row at the top of the existing settings modal.
- Added localStorage persistence under key organon_force_mobile_view.
- Added a body.force-mobile-view class toggle.
- Added CSS so the full hub shell, L1/L2/L3 controls, and basin iframe render at phone-width when enabled.
- The iframe width is narrowed so loaded mini-apps should hit their mobile layouts even inside desktop-like Android WebViews.

No tool routes, categories, app links, split-button routing, or existing storage/import/export actions were changed.
