import { Workspace } from './workspace.js';
import { extensionOf, iconForEntry, typeForFile } from './filesystem.js';

const VERSION = 'v0.34.0 — Stable Settings & Type Database';
const LABEL_KEY = 'organon-capsularius-type-labels-v034';
const BACKUP_KEYS = ['organon-capsularius-sidebar-width-v029','organon-capsularius-column-widths-v029','organon-capsularius-type-labels-v029','organon-capsularius-type-labels-v033','organon-capsularius-type-labels-v034','organon-capsularius-file-metadata-v029','organon-capsularius-mount-location-v1'];
const TYPES = {
  jpg:['🖼️','JPEG Image · .jpg'],jpeg:['🖼️','JPEG Image · .jpeg'],png:['🖼️','PNG Image · .png'],gif:['🖼️','GIF Image · .gif'],webp:['🖼️','WebP Image · .webp'],bmp:['🖼️','Bitmap Image · .bmp'],avif:['🖼️','AVIF Image · .avif'],svg:['🖼️','SVG Vector · .svg'],tif:['🖼️','TIFF Image · .tif'],tiff:['🖼️','TIFF Image · .tiff'],ico:['🖼️','Icon Image · .ico'],heic:['🖼️','HEIC Image · .heic'],
  mp3:['🎵','MP3 Audio · .mp3'],wav:['🎵','WAV Audio · .wav'],ogg:['🎵','Ogg Audio · .ogg'],m4a:['🎵','M4A Audio · .m4a'],flac:['🎵','FLAC Audio · .flac'],aac:['🎵','AAC Audio · .aac'],wma:['🎵','WMA Audio · .wma'],aiff:['🎵','AIFF Audio · .aiff'],mid:['🎼','MIDI File · .mid'],midi:['🎼','MIDI File · .midi'],
  mp4:['🎥','MP4 Video · .mp4'],webm:['🎥','WebM Video · .webm'],mov:['🎥','QuickTime Video · .mov'],mkv:['🎥','Matroska Video · .mkv'],avi:['🎥','AVI Video · .avi'],mpeg:['🎥','MPEG Video · .mpeg'],mpg:['🎥','MPEG Video · .mpg'],m4v:['🎥','M4V Video · .m4v'],
  glb:['📐','3D Model · .glb'],gltf:['📐','3D Model · .gltf'],obj:['📐','3D Model · .obj'],fbx:['📐','3D Model · .fbx'],stl:['📐','3D Model · .stl'],dae:['📐','3D Model · .dae'],blend:['🎲','Blender File · .blend'],unitypackage:['🎮','Unity Package · .unitypackage'],unity:['🎮','Unity Asset · .unity'],uni:['🎮','Data File · .uni'],uasset:['🎮','Unreal Asset · .uasset'],umap:['🎮','Unreal Map · .umap'],pak:['🎮','Game Package · .pak'],
  pdf:['📕','PDF Document · .pdf'],doc:['📘','Word Document · .doc'],docx:['📘','Word Document · .docx'],odt:['📘','OpenDocument Text · .odt'],rtf:['📘','Rich Text Document · .rtf'],xls:['📊','Excel Workbook · .xls'],xlsx:['📊','Excel Workbook · .xlsx'],ods:['📊','OpenDocument Spreadsheet · .ods'],csv:['📊','CSV Data · .csv'],tsv:['📊','TSV Data · .tsv'],ppt:['📙','PowerPoint Presentation · .ppt'],pptx:['📙','PowerPoint Presentation · .pptx'],odp:['📙','OpenDocument Presentation · .odp'],
  ttf:['🔤','TrueType Font · .ttf'],otf:['🔤','OpenType Font · .otf'],woff:['🔤','Web Font · .woff'],woff2:['🔤','Web Font · .woff2'],
  zip:['📦','ZIP Archive · .zip'],rar:['📦','RAR Archive · .rar'],'7z':['📦','7-Zip Archive · .7z'],tar:['📦','TAR Archive · .tar'],gz:['📦','GZip Archive · .gz'],bz2:['📦','BZip2 Archive · .bz2'],iso:['💿','Disc Image · .iso'],exe:['⚙️','Windows Application · .exe'],msi:['⚙️','Windows Installer · .msi'],apk:['⚙️','Android Package · .apk'],
  txt:['📄','Text File · .txt'],md:['📝','Markdown Document · .md'],log:['📄','Log File · .log'],json:['⚙️','JSON Data · .json'],xml:['⚙️','XML Data · .xml'],yaml:['⚙️','YAML Data · .yaml'],yml:['⚙️','YAML Data · .yml'],ini:['⚙️','Configuration · .ini'],cfg:['⚙️','Configuration · .cfg'],conf:['⚙️','Configuration · .conf'],html:['🌐','HTML Document · .html'],htm:['🌐','HTML Document · .htm'],css:['🎨','Stylesheet · .css'],js:['⚙️','JavaScript · .js'],mjs:['⚙️','JavaScript Module · .mjs'],cjs:['⚙️','CommonJS Module · .cjs'],ts:['⚙️','TypeScript · .ts'],tsx:['⚙️','TypeScript React · .tsx'],jsx:['⚙️','JavaScript React · .jsx'],py:['🐍','Python Script · .py'],java:['☕','Java Source · .java'],cs:['⚙️','C# Source · .cs'],cpp:['⚙️','C++ Source · .cpp'],c:['⚙️','C Source · .c'],h:['⚙️','Header File · .h'],php:['⚙️','PHP Script · .php'],sql:['🗄️','SQL Script · .sql'],sh:['⚙️','Shell Script · .sh'],bat:['⚙️','Batch Script · .bat'],ps1:['⚙️','PowerShell Script · .ps1'],
  psd:['🎨','Photoshop File · .psd'],ai:['🎨','Illustrator File · .ai'],aseprite:['🎨','Aseprite File · .aseprite'],kra:['🎨','Krita File · .kra'],xcf:['🎨','GIMP File · .xcf'],db:['🗄️','Database File · .db'],sqlite:['🗄️','SQLite Database · .sqlite'],vdb:['🌊','Volume Data · .vdb'],bin:['📄','Binary Data · .bin'],dat:['📄','Data File · .dat']
};
let activeTab = 'locations';
let queued = false;

function el(tag, className, text) { const item = document.createElement(tag); if (className) item.className = className; if (text !== undefined) item.textContent = text; return item; }
function json(key, fallback) { try { const value = JSON.parse(localStorage.getItem(key) || ''); return value && typeof value === 'object' ? value : fallback; } catch (_) { return fallback; } }
function save(key, value) { try { localStorage.setItem(key, JSON.stringify(value)); } catch (_) { /* no-op */ } }
function workspace() { return window.__capsulariusWorkspace || null; }
function overrides() { return json(LABEL_KEY, {}); }
function descriptor(name) { const extension = extensionOf(name); const custom = String(overrides()[extension] || '').trim(); if (custom) return { extension, icon:TYPES[extension]?.[0] || iconForEntry({kind:'file',name,fileType:typeForFile(name)}), label:custom }; if (TYPES[extension]) return { extension, icon:TYPES[extension][0], label:TYPES[extension][1] }; return { extension, icon:iconForEntry({kind:'file',name,fileType:typeForFile(name)}), label:extension ? `File · .${extension}` : 'File' }; }

function guardBadge() {
  if (window.__capsulariusBadgeGuard) return;
  window.__capsulariusBadgeGuard = true;
  const own = Object.getOwnPropertyDescriptor(Node.prototype, 'textContent');
  if (!own?.get || !own?.set) return;
  Object.defineProperty(Node.prototype, 'textContent', {
    configurable:true,
    enumerable:own.enumerable,
    get() { return own.get.call(this); },
    set(value) {
      const text = value == null ? '' : String(value);
      if (this.nodeType === 1 && this.classList?.contains('app-badge') && /v0\.(29|30|31|32)/.test(text)) return;
      own.set.call(this, value);
    }
  });
}

function styles() {
  if (document.getElementById('caps-v034-style')) return;
  const style = el('style'); style.id = 'caps-v034-style'; style.textContent = `
    .app-badge{font-size:0!important}.app-badge::after{content:'${VERSION}';font:.68rem var(--mono);letter-spacing:.05em;text-transform:uppercase}
    .tree-node{gap:8px!important}.tree-folder-icon{margin-right:4px!important}.tree-node.mount-path-unavailable,.tree-node.mount-path-required{opacity:1!important;filter:none!important}.tree-node.mount-path-unavailable .mount-health,.tree-node.mount-path-required .mount-health{display:none!important}
    .file-item.list.caps-list-row{display:grid!important;min-height:38px!important;height:38px!important;max-height:38px!important;padding:0 10px!important;align-items:center!important;overflow:hidden!important}.file-item.list.caps-list-row .file-item-main{display:flex!important;align-items:center!important;height:38px!important;gap:9px!important;min-width:0!important}.file-item.list.caps-list-row .file-name-wrap{display:block!important;min-width:0!important}.file-item.list.caps-list-row .file-name{display:block!important;overflow:hidden!important;white-space:nowrap!important;text-overflow:ellipsis!important}.file-item.list.caps-list-row .file-subtitle{display:none!important}.file-item.list.caps-list-row .file-icon{flex:none!important;font-size:1.15rem!important;line-height:1!important}.file-item.list.caps-list-row .file-meta,.file-item.list.caps-list-row .caps-list-cell{align-self:center!important;line-height:1.1!important}
    .window-actions .icon-button,.toolbar-button,.view-button,.tree-access-state{border:0!important;background:transparent!important;box-shadow:none!important;filter:drop-shadow(0 3px 4px rgba(0,0,0,.88))}.window-actions .icon-button,.toolbar-button,.view-button{font-size:1.05rem!important}.tree-access-state{padding:0!important;min-width:20px;font-size:1rem!important}
    .caps-v034-backdrop{position:fixed;inset:0;z-index:13000;display:grid;place-items:center;padding:22px;background:rgba(0,0,0,.72);backdrop-filter:blur(4px)}.caps-v034-settings{width:min(900px,calc(100vw - 34px));max-height:calc(100vh - 34px);overflow:auto;border:1px solid #b58244;border-radius:14px;color:#f4e8d3;background:#20211d;box-shadow:0 28px 92px rgba(0,0,0,.84)}.caps-v034-head{position:sticky;top:0;z-index:3;display:flex;justify-content:space-between;gap:14px;padding:15px 20px 0;border-bottom:1px solid rgba(224,163,96,.28);background:#24251f;box-shadow:0 5px 13px rgba(0,0,0,.58)}.caps-v034-title{margin:0 0 13px;font:700 1.1rem var(--head)}.caps-v034-close{border:0;background:transparent;color:#f6ebd9;cursor:pointer;font-size:1.45rem;filter:drop-shadow(0 2px 3px rgba(0,0,0,.8))}.caps-v034-tabs{display:flex;gap:5px;overflow:auto}.caps-v034-tab{border:1px solid transparent;border-bottom:0;border-radius:8px 8px 0 0;padding:9px 12px;color:#c9b79e;background:transparent;cursor:pointer;font:700 .72rem var(--body);letter-spacing:.04em;text-transform:uppercase;white-space:nowrap}.caps-v034-tab.active{border-color:rgba(224,163,96,.5);color:#fff5e3;background:#141511;box-shadow:0 -2px 8px rgba(0,0,0,.38)}.caps-v034-body{padding:18px 20px 22px}.caps-v034-copy{margin:0 0 15px;color:#c7b89f;font-size:.8rem;line-height:1.45}.caps-v034-locations{display:grid;gap:10px}.caps-v034-location{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:12px;padding:13px;border:1px solid rgba(224,163,96,.25);border-radius:9px;background:rgba(0,0,0,.15);box-shadow:0 3px 10px rgba(0,0,0,.3)}.caps-v034-location h3{margin:0;font-size:.9rem}.caps-v034-status{margin:5px 0 0;color:#72d796;font:.68rem var(--mono)}.caps-v034-status.warn{color:#e5a46b}.caps-v034-actions{display:flex;flex-wrap:wrap;justify-content:flex-end;gap:7px}.caps-v034-actions button,.caps-v034-footer button,.caps-v034-backup button,.caps-v034-add button{border:1px solid rgba(224,163,96,.48);border-radius:6px;padding:7px 10px;color:#f4e5cf;background:#31261d;box-shadow:0 3px 9px rgba(0,0,0,.56);cursor:pointer;font:inherit;font-size:.73rem}.caps-v034-actions .danger{color:#ffb6a7;border-color:rgba(206,88,74,.58)}.caps-v034-actions .primary,.caps-v034-footer .primary,.caps-v034-backup .primary{color:#17110a;background:#d49b50;font-weight:800}.caps-v034-footer{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-top:17px}.caps-v034-note{color:#ad9d86;font-size:.71rem}.caps-v034-types{display:grid;gap:8px}.caps-v034-type{display:grid;grid-template-columns:45px 88px minmax(0,1fr);align-items:center;gap:10px;padding:8px 9px;border:1px solid rgba(224,163,96,.18);border-radius:7px;background:rgba(0,0,0,.12)}.caps-v034-type-icon{text-align:center;font-size:1.35rem;filter:drop-shadow(0 2px 3px rgba(0,0,0,.78))}.caps-v034-type code{color:#dbc8aa;font:.75rem var(--mono)}.caps-v034-type input,.caps-v034-add input{width:100%;box-sizing:border-box;border:1px solid rgba(224,163,96,.36);border-radius:5px;padding:7px 8px;color:#f1e5d4;background:#151612;font:inherit;font-size:.75rem}.caps-v034-add{display:grid;grid-template-columns:135px minmax(0,1fr) auto;gap:8px;margin-top:12px}.caps-v034-backup{display:grid;gap:13px;max-width:560px}.caps-v034-backup article{padding:14px;border:1px solid rgba(224,163,96,.25);border-radius:9px;background:rgba(0,0,0,.13)}.caps-v034-backup h3{margin:0 0 7px;font-size:.9rem}.caps-v034-backup p{margin:0 0 12px;color:#c5b69f;font-size:.78rem;line-height:1.45}@media(max-width:620px){.caps-v034-location{grid-template-columns:1fr}.caps-v034-actions{justify-content:flex-start}.caps-v034-add{grid-template-columns:1fr}.caps-v034-type{grid-template-columns:38px 70px minmax(0,1fr)}}
  `; document.head.append(style);
}

function normalizeControls() {
  const controls = {'[data-action="library"]':'📚','[data-action="colour"]':'🎨','[data-action="minimise"]':'➖','[data-action="close"]':'❌','[data-action="back"]':'⬅️','[data-action="forward"]':'➡️','[data-action="up"]':'⬆️','[data-action="refresh"]':'🔄','[data-command="new-folder"]':'📂','[data-view-mode="grid"]':'🔲','[data-view-mode="list"]':'📄'};
  Object.entries(controls).forEach(([selector, emoji]) => document.querySelectorAll(selector).forEach((button) => { if (button.textContent !== emoji) button.textContent = emoji; }));
  document.querySelectorAll('.tree-access-state').forEach((button) => { if (/reconnect/i.test(button.textContent)) { button.textContent = '🔗'; button.title = 'Reconnect'; } });
  const tree = {'▣':'📚','◷':'🕘','G':'☁️','▰':'📁'};
  document.querySelectorAll('.tree-folder-icon').forEach((icon) => { const raw = icon.dataset.v034Raw || icon.textContent.trim(); if (!icon.dataset.v034Raw) icon.dataset.v034Raw = raw; if (tree[raw] && icon.textContent !== tree[raw]) icon.textContent = tree[raw]; });
}

function populateTypes() {
  document.querySelectorAll('.file-item.list.caps-list-row').forEach((row) => {
    const name = row.querySelector('.file-name')?.textContent?.trim(); if (!name) return;
    let cell = row.querySelector('.caps-list-cell.type');
    if (!cell) { cell = el('span','caps-list-cell type'); row.append(cell); }
    const value = descriptor(name);
    if (cell.textContent !== value.label) cell.textContent = value.label;
    const icon = row.querySelector('.file-icon');
    if (icon && !icon.querySelector('img') && icon.textContent !== value.icon) icon.textContent = value.icon;
  });
}

function knownExtensions(ws) {
  const result = new Set(Object.keys(TYPES)); Object.keys(overrides()).forEach((key) => result.add(key));
  for (const windowRecord of ws.state.windows.values()) for (const entry of windowRecord.items || []) { const extension = extensionOf(entry.name); if (extension) result.add(extension); }
  return [...result].sort((a,b) => a.localeCompare(b));
}

function closeSettings() { document.querySelectorAll('.caps-v030-backdrop,.caps-v031-backdrop,.caps-v033-backdrop,.caps-v034-backdrop').forEach((item) => item.remove()); }

function renderLocations(body, ws) {
  body.append(el('p','caps-v034-copy','Mounted Locations checks whether Capsularius can open a saved browser folder. A folder that opens normally is connected; the browser not exposing its Windows path is not an error.'));
  const list = el('div','caps-v034-locations');
  for (const mount of ws.state.mounts.values()) {
    const card = el('article','caps-v034-location'); const left = el('div'); left.append(el('h3','',`📁 ${mount.nickname || mount.name}`));
    const state = mount.locationHealth === 'unavailable' ? 'Unavailable' : mount.locationHealth === 'permission-required' ? 'Reconnect needed' : 'Connected';
    left.append(el('p',`caps-v034-status${state === 'Connected' ? '' : ' warn'}`,`${state} — ${mount.locationDetail || (state === 'Connected' ? 'Folder handle is currently available.' : 'Run scan to check this folder.')}`));
    const actions = el('div','caps-v034-actions'); const relink = el('button','', '📂 Relink'); const remove = el('button','danger','🗑️ Remove');
    relink.addEventListener('click', async () => { try { const handle = await window.showDirectoryPicker({mode:'readwrite'}); mount.handle = handle; mount.name = handle.name || mount.name; mount.permission = await handle.queryPermission({mode:'readwrite'}); mount.locationHealth = mount.permission === 'granted' ? 'connected' : 'permission-required'; mount.locationDetail = mount.permission === 'granted' ? 'Folder opens normally.' : 'Browser permission is needed.'; for (const record of ws.state.windows.values()) ws.renderSidebar(record); ws.onStateChange(); renderSettings(); } catch (error) { if(error?.name !== 'AbortError') ws.onToast(error?.message || 'Folder relink failed.','error'); } });
    remove.addEventListener('click', () => { if (!window.confirm(`Remove “${mount.nickname || mount.name}” from Capsularius?\n\nThe actual folder and files will not be deleted.`)) return; ws.state.mounts.delete(mount.id); ws.state.library = ws.state.library.filter((item) => item.mountId !== mount.id); ws.state.recents = ws.state.recents.filter((item) => item.mountId !== mount.id); for (const record of ws.state.windows.values()) ws.renderSidebar(record); ws.refreshSpecialWindows?.(); ws.onStateChange(); renderSettings(); });
    actions.append(relink,remove);card.append(left,actions);list.append(card);
  }
  body.append(list); const footer = el('div','caps-v034-footer');const controls=el('div'); const mount=el('button','primary','📂 Mount Folder');const scan=el('button','','🔄 Scan Mounted Locations');
  mount.addEventListener('click',()=>{closeSettings();document.getElementById('mount-folder-button')?.click();});scan.addEventListener('click',async()=>{scan.disabled=true;scan.textContent='⏳ Scanning…';for(const mountItem of ws.state.mounts.values()){try{mountItem.permission=await mountItem.handle.queryPermission({mode:'readwrite'});if(mountItem.permission==='granted'){await mountItem.handle.values().next();mountItem.locationHealth='connected';mountItem.locationDetail='Folder opens normally.';}else{mountItem.locationHealth='permission-required';mountItem.locationDetail='Browser permission is needed.';}}catch(error){mountItem.locationHealth='unavailable';mountItem.locationDetail=error?.message || 'The mounted folder could not be opened.';}}for(const record of ws.state.windows.values())ws.renderSidebar(record);ws.onStateChange();renderSettings();});controls.append(mount,scan);footer.append(controls,el('span','caps-v034-note','Remove deletes only the Capsularius reference.'));body.append(footer);
}

function renderTypes(body, ws) {
  body.append(el('p','caps-v034-copy','A populated default file-type database is already loaded. Edit any row to override what Capsularius shows in the Type column.'));
  const labels = overrides(); const list = el('div','caps-v034-types');
  knownExtensions(ws).forEach((extension) => { const current = descriptor(`file.${extension}`); const row=el('label','caps-v034-type');row.append(el('span','caps-v034-type-icon',current.icon),el('code','',`.${extension}`));const input=document.createElement('input');input.value=labels[extension]||'';input.placeholder=current.label;input.dataset.extension=extension;row.append(input);list.append(row); });
  body.append(list);const add=el('div','caps-v034-add');const extension=document.createElement('input');extension.placeholder='Extension, e.g. abc';const label=document.createElement('input');label.placeholder='Type label';const addButton=el('button','','＋ Add');addButton.addEventListener('click',()=>{const key=extension.value.trim().replace(/^\./,'').toLowerCase();if(!key)return;labels[key]=label.value.trim()||`File · .${key}`;save(LABEL_KEY,labels);renderSettings();});add.append(extension,label,addButton);body.append(add);const footer=el('div','caps-v034-footer');const saveButton=el('button','primary','💾 Save File Type Labels');saveButton.addEventListener('click',()=>{body.querySelectorAll('[data-extension]').forEach((input)=>{const value=input.value.trim();if(value)labels[input.dataset.extension]=value;else delete labels[input.dataset.extension];});save(LABEL_KEY,labels);populateTypes();ws.onToast('File type labels saved.','success');});footer.append(saveButton,el('span','caps-v034-note','Leave a field blank to use the current database default.'));body.append(footer);
}

function downloadBackup(){const settings={};BACKUP_KEYS.forEach((key)=>{const value=localStorage.getItem(key);if(value!==null)settings[key]=value;});const blob=new Blob([JSON.stringify({version:1,exportedAt:new Date().toISOString(),settings},null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download=`capsularius-settings-${new Date().toISOString().slice(0,10)}.json`;link.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
function renderBackup(body){body.append(el('p','caps-v034-copy','Export or import Capsularius visual settings, type labels and local metadata. Browser folder permission handles are not exported, so imported mounted locations still need reconnecting.'));const backup=el('div','caps-v034-backup');const exportCard=el('article');exportCard.append(el('h3','','Export Settings'),el('p','','Download the current local Capsularius settings as JSON.'));const download=el('button','primary','⬇️ Download Settings JSON');download.addEventListener('click',downloadBackup);exportCard.append(download);const importCard=el('article');importCard.append(el('h3','','Import Settings'),el('p','','Import a previous Capsularius settings JSON. The app reloads after a valid import.'));const picker=document.createElement('input');picker.type='file';picker.accept='application/json,.json';picker.hidden=true;const upload=el('button','','⬆️ Import Settings JSON');upload.addEventListener('click',()=>picker.click());picker.addEventListener('change',()=>{const file=picker.files?.[0];if(!file)return;const reader=new FileReader();reader.addEventListener('load',()=>{try{const data=JSON.parse(reader.result);if(!data?.settings||typeof data.settings!=='object')throw new Error('This is not a Capsularius settings backup.');Object.entries(data.settings).forEach(([key,value])=>{if(BACKUP_KEYS.includes(key)&&typeof value==='string')localStorage.setItem(key,value);});window.location.reload();}catch(error){window.alert(error?.message || 'The backup could not be imported.');}});reader.readAsText(file);});importCard.append(upload,picker);backup.append(exportCard,importCard);body.append(backup);}

function renderSettings(){const ws=workspace();if(!ws)return;closeSettings();const backdrop=el('div','caps-v034-backdrop');const panel=el('section','caps-v034-settings');const header=el('header','caps-v034-head');const left=el('div');left.append(el('h2','caps-v034-title','Capsularius Settings'));const tabs=el('nav','caps-v034-tabs');[['locations','Mounted Locations'],['types','File Types'],['backup','Backup']].forEach(([id,label])=>{const button=el('button',`caps-v034-tab${activeTab===id?' active':''}`,label);button.type='button';button.addEventListener('click',(event)=>{event.preventDefault();event.stopPropagation();activeTab=id;renderSettings();});tabs.append(button);});left.append(tabs);const close=el('button','caps-v034-close','❌');close.type='button';close.addEventListener('click',(event)=>{event.preventDefault();event.stopPropagation();closeSettings();});header.append(left,close);const body=el('main','caps-v034-body');if(activeTab==='locations')renderLocations(body,ws);else if(activeTab==='types')renderTypes(body,ws);else renderBackup(body);panel.append(header,body);backdrop.append(panel);document.getElementById('dialog-layer').append(backdrop);}

function captureWorkspace(){if(Workspace.prototype.__capsulariusV034Workspace)return;Object.defineProperty(Workspace.prototype,'__capsulariusV034Workspace',{value:true});const original=Workspace.prototype.renderSidebar;Workspace.prototype.renderSidebar=function v034Sidebar(...args){window.__capsulariusWorkspace=this;return original.apply(this,args);};}
function run(){queued=false;normalizeControls();populateTypes();}
function schedule(){if(queued)return;queued=true;requestAnimationFrame(run);}

guardBadge();styles();captureWorkspace();document.addEventListener('click',(event)=>{const settings=event.target.closest('#capsularius-settings-button');if(!settings)return;event.preventDefault();event.stopImmediatePropagation();renderSettings();},true);new MutationObserver(schedule).observe(document.body,{childList:true,subtree:true});setTimeout(schedule,60);
