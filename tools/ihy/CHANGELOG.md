# Ihy v1.0.0

## Clean rebuild

- Consolidated the app into two CSS files and two JavaScript files.
- Corrected the core modal binding crash that left the editor blank.
- Rebuilt the Bass Generator stylesheet against the live HTML IDs/classes.
- Retained the complete Bass Generator controller from the latest full v0.54 implementation.
- Migrates projects from the prior v0.42 and older local-storage keys into the v1 namespace.
- Removed the version-number dependency chain from the runtime file names.
