# Ihy example MIDI source

`potion_song_all_piano_v7.mid` is the supplied three-track piano MIDI example used by the Ihy **Load example** action.

The app also supports importing any standard `.mid` / `.midi` file directly through **Import MIDI / JSON**.