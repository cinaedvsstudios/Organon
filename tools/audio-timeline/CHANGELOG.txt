v0.21
Baseline: v0.20

Files changed:
- advanced.html
- js/advanced-timeline.js
- js/advanced-media-engine.js
- README.txt
- CHANGELOG.txt

Changes:
- Restored the full-height blue now/playhead marker above all timeline lanes.
- Added full-lane clip-end lines in the clip type colour.
- Changed loudness-gradient drawing to select only the source section inside the resized clip.
- Confirmed all media is always played at 1x; resizing only trims/crops the source end.

Rollback: use v0.20 if this pass needs to be reverted.

v0.20
Baseline: v0.19

Files changed:
- advanced.html
- js/advanced-main.js
- README.txt
- CHANGELOG.txt

Changes:
- Replaced duplicate/nested OS File Explorer drop handlers with one guarded import flow.
- The overlay is now visual only and cannot become the drop target.
- Added same-origin parent Hub/iframe listeners so an OS drop arriving on the iframe edge is imported by Advanced Mode.
- Local document, direct-open and Hub iframe usage all cancel the browser default file-open action.

Rollback: use v0.19 if this pass needs to be reverted.

v0.19
- Restored external OS/File Explorer drag-and-drop importing anywhere in Advanced Mode.
- Uses FileList plus DataTransferItem fallback, and listeners on window/document/body/overlay for direct and iframe use.
- Keeps the v0.18 pointer-driven Project Files → timeline drag unchanged.

v0.18
Baseline: v0.17

Files changed:
- advanced.html
- js/advanced-main.js
- js/advanced-timeline.js
- CHANGELOG.txt
- README.txt

Changes:
- Replaced Project Files native HTML drag with a pointer-driven drag system, so dragging file pills into the timeline no longer depends on browser custom MIME handling.
- Added a visible floating drag ghost and live drop highlighting for timeline lanes/panel.
- Restored simple click-to-select behaviour for Project File pills without triggering import.
- Kept the v0.17 lane separation, snapping, end markers, current-time marker, and Basic Mode untouched.

Rollback: use v0.17 if this pass needs to be reverted.
