AUDIO TIMELINE — ADVANCED MODE

v0.22 — Current build

Rollback baseline: v0.21.

Changed files:
- advanced.html
- js/advanced-main.js
- js/advanced-timeline.js

Changes in this pass:
1. Replaced percentage-based loudness gradients with a source-pixel peak strip inside each video/audio clip.
   - The strip is measured from the original source timeline.
   - Right-edge resizing now crops the visible source window.
   - Peak positions do not scale, compress or shift when a clip is trimmed.
   - sourceOffset still shifts the crop correctly after a split.
2. Rebuilt external File Explorer / Finder import handling.
   - One child-document import route for direct Advanced Mode use.
   - A temporary parent-window drop bridge is created when Advanced Mode is embedded in the same-origin Organon hub iframe.
   - The bridge is a real parent-level drop target, so it can receive OS files before the iframe boundary swallows them.
   - External imports remain separate from Project Files -> Timeline dragging.
3. Basic Mode files remain untouched.
