AUDIO TIMELINE — ADVANCED MODE

Version 0.13

Changes
- Added a visible v0.13 badge in the Advanced Audio Timeline header.
- Added a startup toast confirming the loaded Advanced Mode version.
- Removed the Project Files explanatory/drop copy requested in the last pass.
- Rebuilt Project Files as one black Media Bin card containing the draggable file pills. The bin remains clickable and accepts operating-system file drops.
- Added project-wide Canvas Dimensions controls in the left panel. Width and height apply to the actual compositor canvas and update the preview aspect ratio.
- Coalesced live timeline drag compositor redraws to one animation frame, preventing repeated render calls per pointer event.

Rollback baseline
- v0.12 remains untouched in the prior package.
